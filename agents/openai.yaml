interface:
  display_name: "StarPush Social Promo"
  short_description: "Platform-matched promo copy with drafts and publishing"
  default_prompt: "Use $starpushskills-sy to turn a StarPush dream-promotion direction into platform-matched content, save a local draft, or publish only after the current browser connection and account are verified."
policy:
  allow_implicit_invocation: true
