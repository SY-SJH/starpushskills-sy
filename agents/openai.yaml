interface:
  display_name: "StarPush Social Promo"
  short_description: "Platform-matched promo copy with drafts and publishing"
  default_prompt: "Use $starpushskills-sy to turn a StarPush dream-promotion direction into platform-matched content, save a local draft, or publish with the currently signed-in account."
policy:
  allow_implicit_invocation: true
