interface:
  display_name: "StarPush Social Promo"
  short_description: "Platform-matched promo copy with drafts and publishing"
  default_prompt: "Use $starpushskills-sy to turn this product or direction into platform-matched promo content and save drafts or publish with the selected account."
policy:
  allow_implicit_invocation: true
