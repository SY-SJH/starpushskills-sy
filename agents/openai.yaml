interface:
  display_name: "StarPush 推广助手"
  short_description: "按平台生成内容、保存草稿，并在登录后发布"
  default_prompt: "Use $starpushskills-sy to create platform-specific StarPush promotion content, make dream-focused or virtual-character videos with strict real-UI constraints, use the Xiaoyunque API when configured, save drafts, and publish only when requested."
policy:
  allow_implicit_invocation: true
